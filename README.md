To test the code on a non-Risc-V assembly system, download RARS application and run compiled assembly in RARS to test.
Otherwise, in command line, type: "make all" then type ./ptest file_name
The input file must be formatted correctly for the J programming language.
